# All general environment updates should go here

# Set my prompt
NIX_PROMPT_TAG=$(is-nix-shell && echo " [NIX]")
export PS1="\u${NIX_PROMPT_TAG}: \w$ "

# Use vim as default editor
export EDITOR=vim
